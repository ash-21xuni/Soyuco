import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Soyuco — Sign In",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
